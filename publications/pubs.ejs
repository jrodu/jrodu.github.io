```{=html}
<style>
  li {
    margin: 10px 0;
  }
</style>
<ul class="list">
<% for (const item of items) { %>
  <li <%= metadataAttrs(item) %>>
    <%= item.author %> <% if(item.date) { %> (<span class="listing-date"><%= item.date %></span>) <% } %> <span class="listing-title"><a href="<%- item.path %>" class="listing-author"><%= item.title %></a></span>. <span class="listing-details"><%= item.details %></span>.
  </li>
<% } %>
</ul>
</span>
```
